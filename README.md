Enabling an LLM agent to use tools and access external sources (web-links) and add those tokens to its context. This is a Retreival Augmented Generation (RAG) based application. 
The Agent is able to: 
  1. Retreive data from external sources
  2. Add the data to its context
  3. Respond intelligently to users
